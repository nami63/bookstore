#  REST API for a simple Book Store:
	•	Book Schema: title, author, price, publishedDate
	•	Endpoints:
	•	GET /books
	•	POST /books
	•	PUT /books/:id
	•	DELETE /books/:id
	•	MongoDB for data storage.
